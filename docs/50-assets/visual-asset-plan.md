# P0.4 视觉资产与调酒台交互规划

日期：2026-05-21  
阶段：P0.4  
建议版本号：0.0.5

## 1. 当前状态

当前 App 没有真实贴图，调酒台、酒瓶、杯子、量杯、冰块、薄荷和气泡都由 SwiftUI 的 `Shape`、渐变、描边和系统图标绘制。

这些内容适合交互验证，但不足以支撑最终产品的视觉表现。后续需要逐步替换为统一风格的 PNG 或矢量资源。

## 2. P0.4 目标

P0.4 的目标是把调酒台从“按钮表单”调整为“吧台物件操作”：

- 材料从按钮网格改为吧台陈列
- 用户按住酒瓶或材料物件开始倒入
- 冰桶作为吧台物件存在
- 量杯和成品杯保持视觉中心
- 底部只保留工具动作，例如量杯入杯、摇酒、出杯

## 3. 视觉风格方向

建议风格：

- 半写实
- 温暖酒吧灯光
- 玻璃和液体质感突出
- UI 低调，不抢调酒物件
- 角色和经营系统后续可轻度风格化

避免：

- 过度卡通
- 过度写实导致资产成本过高
- 大面积纯按钮界面
- 用系统图标替代关键调酒物件

## 4. MVP 必需贴图

第一批贴图只做 Mojito 所需物件。

| 资源名 | 用途 | 建议尺寸 |
| --- | --- | --- |
| `bar_background` | 酒吧背景 | 1536x2048 |
| `bar_counter` | 吧台台面 | 1536x420 |
| `bottle_white_rum` | 白朗姆瓶 | 512x768 |
| `bottle_lime_juice` | 青柠汁瓶 | 512x768 |
| `bottle_syrup` | 糖浆瓶 | 512x768 |
| `bottle_soda` | 苏打水瓶 | 512x768 |
| `jigger_empty` | 空量杯 | 512x768 |
| `highball_glass_empty` | 空杯 | 512x768 |
| `ice_cube` | 冰块 | 256x256 |
| `mint_leaf` | 薄荷叶 | 256x256 |
| `lime_slice` | 青柠装饰 | 256x256 |

## 4.1 已生成资产

第一批已接入 Xcode Asset Catalog：

| 资源名 | 当前状态 | 说明 |
| --- | --- | --- |
| `highball_glass_empty` | 已生成并接入 | 用于成品杯外观，当前仍叠加 SwiftUI 液体层 |
| `jigger_empty` | 已生成并接入 | 当前作为量杯质感底图，刻度和液面仍由 SwiftUI 绘制 |
| `bottle_white_rum` | 已生成并接入 | 用于白朗姆材料物件 |
| `ice_cube` | 已生成并接入 | 用于冰桶、杯中冰块和落冰动画 |
| `bottle_lime_juice` | 已生成并接入 | 用于青柠汁材料物件 |
| `bottle_syrup` | 已生成并接入 | 用于糖浆材料物件 |
| `bottle_soda` | 已生成并接入 | 用于苏打水材料物件 |
| `mint_leaf` | 已生成并接入 | 用于薄荷材料物件和杯中薄荷反馈 |
| `lime_slice` | 已生成并接入 | 用于青柠汁加入后的杯口装饰 |
| `highball_mojito_full` | P0.8 已生成并接入 | 首页写实 Mojito 成品杯主视觉 |

生成方式：

- 使用内置 `image_gen` 生成 chroma-key 背景图片
- 使用 `remove_chroma_key.py` 去底生成透明 PNG
- 保存到 `mixcrit/Assets.xcassets`

## 5. 后续替换策略

当前代码中的 SwiftUI 占位物件应逐步替换为图片：

- `BarIngredientObjectView` -> 酒瓶和薄荷贴图
- `IceBucketObjectView` -> 冰桶贴图
- `JiggerView` -> 量杯贴图 + SwiftUI 刻度层
- `CocktailGlassView` -> 杯具贴图 + SwiftUI 液体层

推荐不要一次性替换全部。优先替换：

1. 成品杯
2. 量杯
3. 白朗姆瓶
4. 冰块
5. Mojito 剩余材料
6. 吧台背景

## 8. P0.8 写实资产替换记录

更新时间：2026-06-16

已替换：

- `jigger_empty`：替换为写实金属 jigger 透明 PNG。
- `highball_mojito_full`：新增写实 Mojito 成品杯透明 PNG，用于首页预览。
- 首页 `BarCounterPreview`：从插画式瓶子/吧台预览改为写实 jigger + 写实 Mojito 成品杯。

暂缓：

- `highball_glass_empty` 暂不替换。原因是透明玻璃资产使用 chroma-key 去背景后容易保留背景色折射，生成结果带明显洋红色玻璃反光，直接替换会比当前版本更差。

后续建议：

- 若要让调酒页成品杯达到参考图质感，需要使用真正透明背景的玻璃素材，或手工/专业工具制作分层资产。
- 成品杯可拆成 `glass_shell`、`mojito_liquid`、`ice_layer`、`mint_layer`、`lime_layer`、`bubble_layer`，由 SpriteKit 按进度显示。
- 底部原料瓶下一步统一成同一摄影光源和透视角度。

## 6. 交互优化方向

当前 P0.4 交互规则：

- 按住酒瓶倒入量杯
- 按住苏打水直接倒入杯中
- 按住薄荷按片加入杯中
- 点击冰桶加冰
- 点击量杯入杯
- 点击摇酒和出杯

后续优化：

- 拖动量杯到杯子上方倒入
- 拖动冰块到杯中
- 拖动薄荷到杯中
- 长按酒瓶时显示瓶身倾斜动画
- 倒入时显示更真实的液体流

## 7. P0.4 验收标准

- 调酒台看起来像吧台，而不是表单
- 材料以物件形式陈列
- 用户可以通过按住物件完成核心操作
- 量杯和成品杯仍然是视觉中心
- 代码结构方便后续替换为贴图资源
